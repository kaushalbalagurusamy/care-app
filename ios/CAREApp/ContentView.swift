import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationStack {
            ZStack {
                Color(.systemBackground)
                    .ignoresSafeArea()
                
                VStack {
                    // Container ready for Figma design token & component imports
                }
            }
        }
    }
}

#Preview {
    ContentView()
}
